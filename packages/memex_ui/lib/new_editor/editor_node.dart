import 'package:memex_ui/memex_ui.dart';

/*
abstract class EditorNode extends StatelessWidget {
  const EditorNode({
    super.key,
    required this.context,
  });

  EditorNode copyWith({
    Key? key,
    EditorContext? context,
  });

  EditorNode buildNodes(EditorContext context);

  Widget build(BuildContext buildContext);

  @override
  State<StatefulWidget> createState() => _EditorNodeState();
}

class _EditorNodeState extends State<EditorNode> {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}

abstract class EditorInnerNode extends EditorNode {
  final IList<EditorNode> children;
  const EditorInnerNode(
    this.children, {
    super.key,
    required super.context,
  });

  @override
  EditorNode copyWith({
    Key? key,
    EditorContext? context,
    IList<EditorNode>? children,
  });
}

abstract class EditorLeaf extends EditorNode {
  const EditorLeaf({
    super.key,
    required super.context,
  });

  @override
  EditorNode copyWith({
    Key? key,
    EditorContext? context,
  });
}*/
